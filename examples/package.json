{
  "name": "composable-indexes-examples",
  "version": "1.0.0",
  "description": "",
  "module": "commonjs",
  "scripts": {
    "build": "tsc",
    "runAll": "set -o errexit; npm run build && for f in ./dist/*.js; do echo \": $f\"; node $f; done"
  },
  "dependencies": {
    "composable-indexes": "workspace:*"
  },
  "author": "Utku Demir",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^2.8.8",
    "typescript": "^5.1.3"
  },
  "prettier": {
    "proseWrap": "always"
  }
}
